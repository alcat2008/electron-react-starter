document.addEventListener('DOMNodeInserted', () => {
  // TODO. do something, such as preload jquery script
  // if (!!window && !(window.$)) {
  //   window.$ = window.jQuery = require('../jquery.min')
  // }

});
